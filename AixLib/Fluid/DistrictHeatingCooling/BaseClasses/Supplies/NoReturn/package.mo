within AixLib.Fluid.DistrictHeatingCooling.BaseClasses.Supplies;
package NoReturn "Base classes to model only the supply network of DHC systems (without return lines)"
extends Modelica.Icons.BasesPackage;

annotation (Documentation(info="<html><p>
  The base classes in this package provide interfaces for simplidfied
  supply and demand node representations that do not consider the
  return part of the network but only the supply network.
</p>
</html>"));
end NoReturn;
