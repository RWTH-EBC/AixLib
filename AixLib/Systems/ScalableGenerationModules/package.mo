within AixLib.Systems;
package ScalableGenerationModules "Holds module based devices to build energysystems"
  extends Modelica.Icons.VariantsPackage;
  annotation (Documentation(info="<html>
<p>This package contains modular and scalable generation models. The goal is to provide modules instead of only models, which already contain a basic control and basic hydraulic components so that modules can be put together into a system. In most cases the models will already be scalable.</p>
</html>"));
end ScalableGenerationModules;
